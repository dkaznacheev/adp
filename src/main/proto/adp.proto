syntax = "proto3";

message Operation {
  bytes op = 1;
}

message Value {
  bytes value = 1;
}

message ShuffleInfo {
  int32 worker = 1;
  int32 shuffleId = 2;
}

service Worker {
  rpc execute(Operation) returns (Value) {}
  rpc shuffleRead(ShuffleInfo) returns (stream Value) {}
}